name: Assignment check

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    uses: sd5913/pfad/.github/workflows/check-assignment.yml@2026
    with:
      assignment: "1"
